name: chaoswebapp
metadata:
  template: ChaosWebApp
services:
  chaoswebapp:
    project: ./src/ChaosWebApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../Dockerfile
      context: ../..
