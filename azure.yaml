name: chaoswebapp
metadata:
  template: ChaosWebApp
infra:
  parameters:
    hostType: ${AZURE_HOST_TYPE:=containerapp}
services:
  chaoswebapp:
    project: ./src/ChaosWebApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../Dockerfile
      context: ../..
